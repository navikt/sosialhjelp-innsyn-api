#properties to use in integration tests

spring:
  main:
    banner-mode: 'off'

server:
  servlet:
    context-path: '/soknadsosialhjelp/innsyn-api'

client:
  fiks_digisos_endpoint_url: 'example.url' #
  fiks_svar_ut_endpoint_url: 'svar-ut.com' # https://test.svarut.ks.no/forsendelse/{id}/{nr}
  fiks_dokumentlager_endpoint_url: 'dokumentlager.com' # https://minside.fiks.test.ks.no/dokumentlager/nedlasting/{id}